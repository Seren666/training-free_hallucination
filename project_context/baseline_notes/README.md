# Baseline Notes

This directory is reserved for lightweight baseline run notes and result summaries.

Preferred contents after experiments begin:

- short markdown summaries
- `manifest.json`
- `summary.csv`
- comparison notes that point back to remote raw outputs

Do not place model files, dataset copies, raw `jsonl` outputs, or cache here.

